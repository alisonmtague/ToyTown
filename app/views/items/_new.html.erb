<%= form_for ([@wishlist, @item]) do |f| %>
	<%= f.hidden_field :wishlist_id, :value => @wishlist.id %>
	<%= f.label :item_name %>
	<%= f.text_field :item_name %>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.submit %>
<%end%>