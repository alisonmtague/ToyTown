WISHLIST INDEX

<ul><% @wishlists.each do |wishlist| %>
  <li><%= link_to wishlist.title, wishlist_path(wishlist) %>
	<%= link_to 'delete wishlist', wishlist, method: :delete %></li> 
<%end%></ul>

<%= render 'new' %>
<%#= link_to 'Create a New Wishlist', new_wishlist_path %>

